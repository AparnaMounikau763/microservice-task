apiVersion: apps/v1
kind: Deployment
metadata:
  name: task-manager

spec:
  replicas: 2

  selector:
    matchLabels:
      app: task-manager

  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1

  template:
    metadata:
      labels:
        app: task-manager

    spec:
      containers:
      - name: app

        image: aparnamounika/task-manager:8c2fc0bc5dac2aebe85c1a8e948473ee7e0d8ec2
        imagePullPolicy: Always

        ports:
        - containerPort: 3000

        env:
        - name: MONGO_URL
          value: mongodb://mongo:27017/tasks

        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"

          limits:
            memory: "256Mi"
            cpu: "200m"

        readinessProbe:
          httpGet:
            path: /tasks
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10

        livenessProbe:
          httpGet:
            path: /tasks
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 20